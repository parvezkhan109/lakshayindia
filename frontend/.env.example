# Frontend
# If empty, the app will call same-origin endpoints like /api/... (best when you reverse-proxy on the same domain).
# If you host API separately, set it like: https://api.lakshayindia.biz
VITE_API_BASE_URL=
